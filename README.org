This is my personal Emacs configuration.

All code and explanation are located in =init.org=. To generate
emacs-lisp configuration code which get executed by Emacs at launch
time, tangle the =init.org= file. For example, execute the command
=org-babel-tangle= in =init.org= buffer.
